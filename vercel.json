{
  "buildCommand": "git clone https://github.com/flutter/flutter.git && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter config --enable-web && flutter build web",
  "outputDirectory": "build/web",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}